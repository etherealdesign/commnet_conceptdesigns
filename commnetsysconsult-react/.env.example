# Canonical origin used in canonical URLs, Open Graph and JSON-LD.
VITE_SITE_URL=https://commnetsysconsult.com
# POST endpoint for the project-brief form (JSON body). Leave empty to fall back to mailto.
VITE_FORM_ENDPOINT=
# Per-project AED values are subcontract information (CONTENT-REVAMP-REVIEW.md §8.3).
# Keep false until the client approves publishing them.
VITE_SHOW_CONTRACT_VALUES=false
